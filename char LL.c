#include <stdio.h>
#include <stdlib.h>
#include <string.h>
//#include <string.h>


int main() {

void main()
{
    node*ptr;
    char word;
    cin>>data;
    ptr=create(data);
}


struct node{
    char data;
    node*next;
}start;
start=NULL;

node*create(char word)
{
    node*ptr;
    ptr = new node;

    if(ptr==NULL)
        exit=1;
    else
    {
     ptr->data=word;
     ptr->next=NULL;
     return ptr;
    }
}

return 0;
}
