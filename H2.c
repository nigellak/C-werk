#include <stdio.h>
#include <stdlib.h>

struct node
{
    int data;          // Data
    struct node *next; // Address
};

struct node *head = NULL;
struct node *next = NULL;


struct stack
{
    int data;
    struct stack *next;
};

struct node {
   int data;
   int key;

   struct node *next;
   struct node *prev;
};

//this link always point to first Link
struct node *head = NULL;

//this link always point to last Link
struct node *last = NULL;

struct node *duble = NULL;

//is list empty
bool isEmpty() {
   return head == NULL;
}

int list['m','s','r','t']

int main{

    insertFirst('m');
   insertFirst('t');
   insertFirst('k');
   insertFirst('d');
   insertFirst('f');



return 0;
}
